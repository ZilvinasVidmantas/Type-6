import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#fc5203',
      light: '#ff6f2b',
      dark: '#e04800',
      contrastText: '#ffffff',
    },
  },
});

export default theme;
