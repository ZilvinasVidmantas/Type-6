import React from 'react';
import {
  Typography,
  Box,
} from '@mui/material';

const App: React.FC = () => (
  <Box component="main">
    <Typography component="h1" variant="h2">React context</Typography>
  </Box>
);

export default App;
